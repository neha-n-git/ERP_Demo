# Port to run the server on
PORT=5000

# Database Connection (MySQL)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=your_mysql_password_here
DB_NAME=sjhif_erp

# JWT Secret for admin authentication
JWT_SECRET=super_secret_jwt_key_sjhif_incubation_system_2026

# JWT Expiration Time
JWT_EXPIRY=7d

# SMTP Configuration (Optional, falls back to server console logs if empty)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM="SJRI Incubation System <no-reply@sjhif.in>"
